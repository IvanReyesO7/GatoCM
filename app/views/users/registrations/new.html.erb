<body style="background-color: #F0F0F0">
  <div id="main-form-container" class="text-center">
    <div class="form-signin">
      <%= image_tag "logo.png", alt: "GatoCmM's logo", class: "mb-4", width: "300" %>
      <h1 class="h3 mb-3 font-weight-normal">Get started!</h1>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= f.email_field :email, autofocus: true, required: true, autocomplete: "email", class: "form-control", placeholder: "Email address" %>
        <%= f.text_field :username, autofocus: true, required: true, autocomplete: "username", class: "form-control", placeholder: "Username" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "form-control sign-up-password", placeholder: "Password (#{@minimum_password_length} characters minimum)", required: true %>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control sign-up-password-confirmation", placeholder: "Password (#{@minimum_password_length} characters minimum)", required: true %>
        <%= f.submit "Create an account", class: "btn btn-lg btn-dark btn-block" %>
        <p class="mt-5 mb-3 text-muted">&copy; GatoCM - 2023</p>
      <% end %>
    </div>
  </div>
</body>
<style>
  html,
  body {
    height: 100%;
  }

  #main-form-container {
    display: -ms-flexbox;
    display: -webkit-box;
    display: flex;
    -ms-flex-align: center;
    -ms-flex-pack: center;
    -webkit-box-align: center;
    align-items: center;
    -webkit-box-pack: center;
    justify-content: center;
    padding-top: 40px;
    padding-bottom: 40px;
  }

  .form-signin {
    width: 100%;
    max-width: 330px;
    padding: 15px;
    margin: 0 auto;
  }
  .form-signin .checkbox {
    font-weight: 400;
  }
  .form-signin .form-control {
    position: relative;
    box-sizing: border-box;
    height: auto;
    padding: 10px;
    font-size: 16px;
  }
  .form-signin .form-control:focus {
    z-index: 2;
  }
  .form-signin input[type="email"] {
    margin-bottom: -1px;
    border-bottom-right-radius: 0;
    border-bottom-left-radius: 0;
  }
  .form-signin input[type="text"] {
    margin-bottom: -1px;
    border-radius: 0;
    border-radius: 0;
  }
  .sign-up-password {
    margin-bottom: -1px;
    border-radius: 0;
  }
  .sign-up-password-confirmation {
    margin-bottom: 10px;
    border-top-right-radius: 0;
    border-top-left-radius: 0;
  }
</style>